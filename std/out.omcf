-- tellraw("@a", "Hello World!")
-- this function will change to JSON format
fu tellraw(selector, text) {
    run("tellraw " + selector + " "" + text + ")
}

-- say("Hello World")
fu say(text) {
    run("say " + text)
}
